[package]
name = "onionroute-policy-engine"
version = "0.1.0"
description = "Fail-closed local routing policy for OnionRoute"
edition = "2021"
license = "Apache-2.0"
rust-version = "1.78"

[dependencies]
onionroute-common-types = { path = "../common-types" }

[dev-dependencies]
onionroute-common-types = { path = "../common-types", features = ["test-utils"] }
